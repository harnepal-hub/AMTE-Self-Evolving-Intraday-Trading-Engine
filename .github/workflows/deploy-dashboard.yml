name: Deploy AMTE Dashboard

# Single Pages deployment is maintained by deploy-dashboard-live.yml.
# Keep this legacy workflow manual-only to prevent competing deployments.
on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy dashboard deployment disabled; use Deploy AMTE Dashboard Live."
