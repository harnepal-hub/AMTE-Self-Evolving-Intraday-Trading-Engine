name: Legacy TW Multi-Coin Paper Diagnostics

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow retained for manual diagnostics. Server-side paper engine is now the active runner."
